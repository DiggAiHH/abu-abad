# Legacy Backend (DEPRECATED)

Dieses Verzeichnis `backend/` ist **alt/legacy** und wird **nicht** mehr für Deployments verwendet.

Aktuelles, gehärtetes Backend (v8):
- `apps/backend/`

Wichtig:
- `docker-compose.yml` nutzt ausschließlich `apps/backend`.
- Verwende für Entwicklung/Deployment die Dokumentation unter `apps/backend` und die Root-Skripte (`start-services.sh`, `start-all.sh`).
